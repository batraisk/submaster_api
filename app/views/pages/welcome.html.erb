<% @content.gsub! '{{image}}', rails_blob_path(@page.background, only_path: true) if @page.background.attached?%>
<% @content.gsub! '{{youtube}}', '' unless @page.youtube%>
<%= @content.html_safe %>
<%= csrf_meta_tags %>

<%= javascript_pack_tag 'pages' %>
<script type="text/javascript" charset="utf-8">
  saveId('<%= @guest.hashid %>')
</script>

