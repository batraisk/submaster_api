<% base_url = Rails.env.development? ? 'http://localhost:4200' : 'https://submaster.pro' %>
<p><%= t 'auth.welcome' %> <%= @email %>!</p>

<p><%= t 'auth.confirm_account' %>:</p>

<!--<p><%#= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token).split('/').take(3).join('/') + "/#/auth/login?confirmation_token=#{@token}" %></p>-->
<p><%= link_to t('auth.confirm'), "#{base_url}/auth/login?confirmation_token=#{@token}" %></p>
